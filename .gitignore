# Dépendances (l'app est 100% vanilla JS servie en statique — node_modules n'est
# PAS utilisé à l'exécution ; vestige de l'essai Vite/Quagga2 abandonné).
node_modules/

# Sortie générée par graphify (regénérable via /graphify, ne pas déployer).
graphify-out/

# Skills Claude installés (outillage perso, réinstallables ; garder settings.json).
.claude/skills/
.claude/settings.local.json
skills-lock.json

# Divers
.DS_Store
*.log
